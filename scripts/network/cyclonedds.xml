<?xml version="1.0" encoding="UTF-8"?>
<CycloneDDS xmlns="https://cdds.io/config" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="https://cdds.io/config https://raw.githubusercontent.com/eclipse-cyclonedds/cyclonedds/master/etc/cyclonedds.xsd">
  <Domain>
    <General>
      <AllowMulticast>true</AllowMulticast>
      <!-- IMPORTANT: keep MaxMessageSize BELOW the real network MTU (1500 on
           typical Ethernet/WiFi). CycloneDDS docs: "the maximum size of the
           UDP payload that Cyclone DDS will generate... On some networks it
           may be necessary to set this item to keep the packet size below
           the MTU to prevent IP fragmentation." Raising this to MB-scale
           values causes EMSGSIZE (DDS_RETCODE_NOT_ENOUGH_SPACE / retcode -58)
           on every large message; raising it only to just-under-MTU values
           (e.g. the 14720 B upstream default) forces fragile OS-level IP
           fragmentation that WiFi tends to drop, silently killing large
           topics (images/pointclouds). DDSI-level FragmentSize already
           chops large samples into small pieces - do not raise
           MaxMessageSize to "fix" large message delivery. -->
      <MaxMessageSize>1470 B</MaxMessageSize>
      <FragmentSize>1200 B</FragmentSize>
    </General>
    <Internal>
      <SocketReceiveBufferSize min="10MB"/>
      <SocketSendBufferSize min="10MB"/>
    </Internal>
  </Domain>
</CycloneDDS>
