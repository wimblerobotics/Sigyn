╔══════════════════════════════════════════════════════════════════════════╗
║                    RVIZ2 SSH FORWARDING FIX - SUMMARY                    ║
║                         Completed: 2026-02-21                            ║
╚══════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────┐
│ PHASE 1: SIGYN7900A CONFIGURATION ✅ COMPLETED                           │
└──────────────────────────────────────────────────────────────────────────┘

✓ Removed LIBGL_ALWAYS_SOFTWARE=1 from bashrc:
  - Fixed `map` alias
  - Fixed `nav` alias

ACTION REQUIRED: Deploy to sigyn7900a
┌──────────────────────────────────────────────────────────────────────────┐
│ scp ~/sigyn_ws/src/Sigyn/bashrc sigyn7900a:~/.bash_aliases              │
│ ssh sigyn7900a 'source ~/.bash_aliases'                                  │
└──────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│ PHASE 2: AMDC NVIDIA DRIVER UPGRADE ✅ COMPLETED                         │
└──────────────────────────────────────────────────────────────────────────┘

✓ Upgraded from driver 535.288.01 → 590.48.01 (open source)
✓ DKMS modules built successfully for kernel 6.8.0-100-generic
✓ All packages installed without errors

GPU: NVIDIA GeForce RTX 3060
New Driver: nvidia-driver-590-open (Ubuntu recommended)

┌──────────────────────────────────────────────────────────────────────────┐
│ IMMEDIATE NEXT STEP: REBOOT AMDC ⚠️ REQUIRED                            │
└──────────────────────────────────────────────────────────────────────────┘

The new driver will not be active until you reboot.

To reboot now:
┌──────────────────────────────────────────────────────────────────────────┐
│ sudo reboot                                                              │
└──────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│ PHASE 3: POST-REBOOT VALIDATION                                          │
└──────────────────────────────────────────────────────────────────────────┘

After amdc reboots, run the validation script:
┌──────────────────────────────────────────────────────────────────────────┐
│ ~/sigyn_ws/src/Sigyn/scripts/validate_nvidia_driver.sh                  │
└──────────────────────────────────────────────────────────────────────────┘

Or manually check:
┌──────────────────────────────────────────────────────────────────────────┐
│ nvidia-smi                          # Should show 590.48.01              │
│ glxinfo | grep "OpenGL renderer"    # Should show NVIDIA, not Mesa      │
└──────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│ PHASE 4: TEST SSH X FORWARDING FROM SIGYN7900A                          │
└──────────────────────────────────────────────────────────────────────────┘

From sigyn7900a terminal:
┌──────────────────────────────────────────────────────────────────────────┐
│ # 1. Test GLX info                                                       │
│ ssh -Y amdc 'glxinfo | grep renderer'                                    │
│                                                                          │
│ # 2. Test spinning gears                                                 │
│ ssh -Y amdc glxgears                                                     │
│                                                                          │
│ # 3. THE BIG TEST: rviz2                                                 │
│ ssh -Y amdc 'source ~/.bash_aliases && rviz2'                           │
└──────────────────────────────────────────────────────────────────────────┘

Expected Results:
  ✓ No session crashes
  ✓ rviz2 starts and displays correctly
  ✓ Hardware acceleration enabled (smooth rendering)

┌──────────────────────────────────────────────────────────────────────────┐
│ PHASE 5: VIRTUALGL + TURBOVNC (IF X11 FORWARDING STILL UNSTABLE)        │
└──────────────────────────────────────────────────────────────────────────┘

If SSH X forwarding still has issues, use the more stable VNC approach.

Installation (on amdc, after reboot):
┌──────────────────────────────────────────────────────────────────────────┐
│ wget https://sourceforge.net/projects/virtualgl/files/3.1/virtualgl_3.1_amd64.deb
│ sudo dpkg -i virtualgl_3.1_amd64.deb                                    │
│ sudo apt-get install -f                                                  │
│                                                                          │
│ wget https://sourceforge.net/projects/turbovnc/files/3.1.1/turbovnc_3.1.1_amd64.deb
│ sudo dpkg -i turbovnc_3.1.1_amd64.deb                                   │
│ sudo apt-get install -f                                                  │
│                                                                          │
│ sudo /opt/VirtualGL/bin/vglserver_config                                │
│ # Select option 1, answer Yes to all prompts                            │
└──────────────────────────────────────────────────────────────────────────┘

Usage:
┌──────────────────────────────────────────────────────────────────────────┐
│ On amdc:                                                                 │
│   /opt/TurboVNC/bin/vncserver :1 -geometry 1920x1080                   │
│                                                                          │
│ On sigyn7900a:                                                          │
│   ssh -L 5901:localhost:5901 amdc  # Keep this terminal open           │
│   vncviewer localhost:5901          # In another terminal              │
│                                                                          │
│ In VNC session:                                                          │
│   source ~/.bash_aliases                                                 │
│   vglrun rviz2                                                          │
└──────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│ TROUBLESHOOTING RESOURCES                                                │
└──────────────────────────────────────────────────────────────────────────┘

Helper Scripts Created:
  • ~/sigyn_ws/src/Sigyn/scripts/fix_rviz_ssh_forwarding.sh
    Interactive menu for all driver tasks
    
  • ~/sigyn_ws/src/Sigyn/scripts/validate_nvidia_driver.sh
    Post-reboot validation checks

Documentation:
  • ~/sigyn_ws/src/Sigyn/Documentation/RVIZ_SSH_FORWARDING_FIX.md
    Complete detailed guide with all commands and explanations

Quick Diagnostics:
┌──────────────────────────────────────────────────────────────────────────┐
│ lsmod | grep nvidia              # Check if modules loaded              │
│ dmesg | grep -i nvidia           # Check for kernel messages            │
│ cat /var/log/Xorg.0.log | grep NVIDIA  # Check X server log             │
└──────────────────────────────────────────────────────────────────────────┘

Common Issues:
  
  Problem: "Cannot open display"
  → Ensure using `ssh -Y` (not just `ssh -X`)
  → Check: echo $DISPLAY in SSH session
  
  Problem: Still showing Mesa/llvmpipe
  → Check nvidia-smi works
  → Try: __GLX_VENDOR_LIBRARY_NAME=nvidia rviz2
  → May need to disable Wayland (use X11 session)
  
  Problem: rviz2 crashes immediately
  → Check for conflicting GL libraries
  → Consider VirtualGL+TurboVNC (more stable)

┌──────────────────────────────────────────────────────────────────────────┐
│ CHANGES SUMMARY                                                          │
└──────────────────────────────────────────────────────────────────────────┘

Files Modified:
  ✓ bashrc - Removed LIBGL_ALWAYS_SOFTWARE=1 from map and nav aliases

Files Created:
  ✓ scripts/fix_rviz_ssh_forwarding.sh - Interactive helper
  ✓ scripts/validate_nvidia_driver.sh - Post-reboot validation
  ✓ Documentation/RVIZ_SSH_FORWARDING_FIX.md - Complete guide
  ✓ Documentation/NEXT_STEPS.txt - This file

System Changes:
  ✓ NVIDIA driver upgraded: 535 → 590-open
  ✓ Kernel modules built for 6.8.0-100-generic
  ⚠️ REBOOT REQUIRED to activate new driver

┌──────────────────────────────────────────────────────────────────────────┐
│ STATUS CHECKLIST                                                         │
└──────────────────────────────────────────────────────────────────────────┘

  [✓] Remove Mesa forcing from bashrc
  [⚠] Deploy bashrc to sigyn7900a (manual step required)
  [✓] Upgrade NVIDIA driver on amdc
  [⚠] Reboot amdc (manual step required)
  [ ] Validate driver with nvidia-smi
  [ ] Validate GLX with glxinfo
  [ ] Test SSH X forwarding with glxgears
  [ ] Test rviz2 via SSH from sigyn7900a
  [ ] (Optional) Setup VirtualGL+TurboVNC if needed

┌──────────────────────────────────────────────────────────────────────────┐
│ 🎯 YOUR IMMEDIATE ACTION: REBOOT AMDC                                    │
└──────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════

For questions or issues, refer to:
  Documentation/RVIZ_SSH_FORWARDING_FIX.md

Good luck! 🚀
