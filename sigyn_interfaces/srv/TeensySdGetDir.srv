# Request: no parameters needed - always returns root directory listing
# (left empty for future extensibility)

---

# Response: directory listing result
string directory_listing
bool success
string error_message
